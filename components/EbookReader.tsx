import React from 'react'

interface ReaderProps {
    productPath: string
}

const EbookReader = ({ productPath }: ReaderProps) => {
    return (
        <div>
            {/* TO-DO: (IDEA) Implement shadcn progress bar */}
            Reader
        </div>
    )
}

export default EbookReader
